# Default configuration for alpsnap

SNAP_ROOT="/snapshots"

SNAP_BACKEND="auto"

INCLUDE_PATHS="/etc /var /home /root"
EXCLUDE_PATHS="/proc /sys /dev /run /tmp /mnt /media /lost+found"

LOG_FILE="/var/log/alpsnap.log"

TIME_FMT="%Y%m%d-%H%M%S"
